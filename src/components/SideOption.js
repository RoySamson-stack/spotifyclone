import React from 'react'

function SideBarOption({title, icon}) {
  return (
    <div>
      	<h4 className="font-semibold text-gray-400 hover:text-white">{title}</h4>
    </div>
  )
}

export default SideBarOption
